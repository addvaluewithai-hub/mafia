name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22.13
      - name: Install dependencies from lockfile
        run: npm ci
      - name: Production dependency security gate
        run: npm audit --omit=dev --audit-level=high
      - name: Expo doctor
        run: npx expo-doctor
      - name: TypeScript
        run: npm run typecheck
      - name: CI toolchain pinning contract
        run: node scripts/qa/ci-toolchain-pinning-contract.mjs
      - name: Release preflight contract
        run: npm run qa:release-preflight
      - name: Observability privacy and operations contracts
        run: npm run qa:observability
